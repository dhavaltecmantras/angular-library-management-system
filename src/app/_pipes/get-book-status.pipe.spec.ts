import { GetBookStatusPipe } from './get-book-status.pipe';

describe('GetBookStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new GetBookStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
